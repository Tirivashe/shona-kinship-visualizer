/**
 * @deprecated The application now uses `InMemoryFamilyDatabase`. These seed
 * exports remain only for older imports outside the UI.
 */
export {
  initialPeople as samplePeople,
  initialRelationships as sampleRelationships,
} from "./in-memory-family-database";
